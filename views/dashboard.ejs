<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= t.nav.dashboard %> - Kaitlus</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-brand">
            <a href="/">KAITLUS</a>
        </div>
        <div class="nav-menu">
            <a href="/"><%= t.nav.home %></a>
            <a href="/order"><%= t.nav.order %></a>
            <a href="/services"><%= t.nav.services %></a>
            <a href="/sales"><%= t.nav.sales %></a>
            <a href="/contact"><%= t.nav.contact %></a>
        </div>
        <div class="nav-links">
            <%- include('partials/lang-switcher') %>
            <span class="welcome-text">Hi, <%= user.username %></span>
            <a href="/auth/logout" class="btn btn-outline"><%= t.nav.logout %></a>
        </div>
    </nav>

    <main class="dashboard">
        <div class="dashboard-header">
            <h1><%= t.dashboard.welcome %>, <%= user.username %>!</h1>
            <p><%= t.dashboard.manageAccount %></p>
        </div>

        <div class="dashboard-cards">
            <% if (user.is_admin) { %>
            <div class="dashboard-card" style="background: linear-gradient(135deg, #1e293b 0%, #334155 100%); color: white; border: 2px solid #f59e0b;">
                <h3>🔐 Admin Panel</h3>
                <p>Access admin features and view customer chat conversations.</p>
                <p style="margin-top: 1rem;">
                    <a href="/admin/chats" class="btn btn-success">View Chat Logs</a>
                </p>
            </div>
            <% } %>

            <div class="dashboard-card">
                <h3>👤 <%= t.dashboard.profile %></h3>
                <p><strong><%= t.auth.username %>:</strong> <%= user.username %></p>
                <p><strong><%= t.auth.email %>:</strong> <%= user.email %></p>
                <p><strong><%= t.dashboard.memberSince %>:</strong> <%= new Date(user.created_at).toLocaleDateString() %></p>
            </div>
            
            <div class="dashboard-card">
                <h3>📦 <%= t.dashboard.myOrders %></h3>
                <p><%= t.dashboard.noOrders %></p>
                <p style="margin-top: 1rem;">
                    <a href="/order" class="btn btn-primary"><%= t.home.orderBtn %></a>
                </p>
            </div>
            
            <div class="dashboard-card">
                <h3>📞 <%= t.dashboard.needHelp %></h3>
                <p><%= t.dashboard.helpDesc %></p>
                <p style="margin-top: 1rem;">
                    <a href="/contact" class="btn btn-outline"><%= t.home.contactUs %></a>
                </p>
            </div>
        </div>
    </main>

    <footer class="footer">
        <div class="footer-bottom">
            <p>&copy; 2025 Kaitlus. <%= t.footer.rights %></p>
        </div>
    </footer>

    <%- include('partials/chat-widget') %>
</body>
</html>
